# FloodGuard

A comprehensive flood control system and dashboard platform. This project provides a monorepo-style structure containing both the frontend web application and backend server.

## Project Structure

- **`web/`**: The frontend Next.js 16 application featuring Framer Motion, Recharts, Shadcn, Base UI, and Tailwind CSS.
- **`server/`**: The backend server logic (currently being initialized).

## Getting Started

### Prerequisites

- Node.js (version 20 or higher recommended)
- npm or yarn

### Running the Web Application

1. Navigate to the web directory:
   ```bash
   cd web
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Run the development server:
   ```bash
   npm run dev
   ```
4. Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

### Features

- Modern UI built with Tailwind CSS v4 and Shadcn
- Dynamic light/dark mode theme system
- Interactive charts and data visualizations using Recharts
- Smooth animations with Framer Motion

## Contribution

For more specifics, please check the individual `README.md` inside each module (`web/`, `server/`).
